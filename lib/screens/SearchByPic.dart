import 'package:flutter/material.dart';

class SearchBy extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Search by picture"),
      ),
      
    );
  }
}